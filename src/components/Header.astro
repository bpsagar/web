---
interface Props {
	title?: string;
}

const { title } = Astro.props;
---

<div class="flex items-center font-host font-medium text-lg py-4">
	<span>sagar.im</span>
	{title && (
		<>
			<span class="text-zinc-400 mx-3">/</span>
			<span class="text-zinc-500">{title}</span>
		</>
	)}
</div>
